# note用リポジトリ

このディレクトリは、note記事の下書き・公開管理用のテンプレートです。

## 構成
- `drafts/`: 下書き
- `published/`: 公開済み記事
- `assets/`: 画像などの素材

## 使い方
1. `drafts/` に `YYYY-MM-DD-title.md` 形式で下書きを作成
2. 公開後に `published/` へ移動
3. 画像は `assets/` に保存して Markdown から相対パスで参照
